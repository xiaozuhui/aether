// examples/lazy_demo.aether
// Lazy 延迟求值演示
//
// 语义（0.6.0 起）：
//   - Lazy 定义时不求值（无副作用）
//   - 第一次读取时强制求值，之后读取直接复用结果（记忆化）
//   - 自引用定义（Lazy Y (Y + 1)）会报「循环定义」错误
//
// 提示：加 --trace 运行本文件（aether examples/lazy_demo.aether --trace），
// 可以看到 "HEAVY" 类目的副作用恰好出现一次——第二次读取不重复求值。

// 1. 带副作用的 Lazy：副作用放在 If 表达式块里（Lazy 体必须是表达式）
Lazy HEAVY (
    If (True) {
        TRACE_INFO("HEAVY", 1)
        100
    } Else {
        0
    }
)

// 第一次读取：触发求值（TRACE 一次）
PRINTLN(HEAVY)          // 100

// 再次读取：记忆化，副作用不重复
PRINTLN(HEAVY + HEAVY)  // 200

// 2. 纯表达式 Lazy：适合延迟昂贵计算
Lazy CONST_VAL (1 / 3)
PRINTLN(TO_FRACTION(CONST_VAL))   // 1/3（精确分数，非 0.333...）
